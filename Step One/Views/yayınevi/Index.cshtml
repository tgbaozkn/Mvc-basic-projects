@model IEnumerable<Kutuphane.Models.Entity.yayınevi>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.yayınevi_ad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.yayınevi_ad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.yayınevi_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.yayınevi_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.yayınevi_id })
        </td>
    </tr>
}

</table>
